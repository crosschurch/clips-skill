# Required for make_vertical.py (face/body tracking + 9:16 crop)
opencv-python>=4.8
numpy>=1.24
scipy>=1.11

# Optional but strongly recommended — 3-4x faster CPU transcription for the
# full sermon. transcribe.sh falls back to plain `whisper` if missing.
faster-whisper>=1.0
